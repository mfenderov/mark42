{
  "mcpServers": {
    "claude-memory": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/claude-memory-server",
      "args": [],
      "env": {
        "CLAUDE_MEMORY_DB": "${HOME}/.claude/memory.db"
      }
    }
  }
}
